import styles from '../styles/Header.module.css';

const Header = () => {
  return (
    <h1 className={styles.title}>
          <span className={styles.span}>drivvn</span> Engineering Blog
    </h1>
  );
}

export default Header; 